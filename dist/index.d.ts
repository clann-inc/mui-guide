import { ReactNode } from "react";
declare type Props = {
    children?: ReactNode;
    disable?: boolean;
};
export default function Guide({ children, disable }: Props): {} | null | undefined;
export {};
