import { ReactNode } from "react";
declare type Props = {
    children?: ReactNode;
    disable?: boolean;
};
declare const Guide: ({ children, disable }: Props) => {} | null | undefined;
export default Guide;
